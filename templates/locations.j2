<!-- Code on this page adapted from bsg-people.j2 in the cs340 materials here: https://canvas.oregonstate.edu/courses/2017561/pages/exploration-web-application-technology-2?module_item_id=25645131 -->

{% extends "main.j2" %}
{% block content %}

<h1> Locations</h1>

<h2> {{message}} </h2>

{# READ table #}
<table>
    <thead>
        {# For the table header row, we print the DB attribute names #}
        <tr>
            {% for header in headers %}
            <th>{{ header }}</th>
            {% endfor %}
        </tr>
    </thead>

    <tbody>
        {% for location in locations %}
        <tr>
            <td>{{ location.locationName }}</td>
            <td>{{ location.locationDescription }}</td>

            {# DELETE form #}
            <form id="delete_location_form" action="/delete" method="POST">
                <input type="hidden" name="table" value="locations">
                <input type="hidden" name="id" value="{{ location.locationID }}">
                <input type="hidden" name="name" value="{{ location.locationName }}">
                <td>
                    <button type="submit">
                        Delete
                    </button>
                </td>
            </form>
        </tr>
        {% endfor %}
    </tbody>
</table>

{% endblock %}